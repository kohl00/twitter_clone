<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id), remote: true,
             html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>

<pre> <%= current_user.active_relationships.find_by(followed_id: @user.id).inspect if Rails.env.development? %> </pre> 